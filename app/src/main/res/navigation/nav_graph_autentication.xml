<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragmentAuthentication">

    <fragment
        android:id="@+id/fragmentAuthentication"
        android:name="com.petscare.org.fragments.FragmentAuthentication"
        android:label="fragment_authentication"
        tools:layout="@layout/frg_authentication" >
        <action
            android:id="@+id/action_fragmentAuthentication_to_fragmentVerification"
            app:destination="@id/fragmentVerification" />
    </fragment>
    <fragment
        android:id="@+id/fragmentVerification"
        android:name="com.petscare.org.fragments.FragmentVerification"
        android:label="fragment_verification"
        tools:layout="@layout/frg_verification" />
</navigation>