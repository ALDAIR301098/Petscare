<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.petscare.org">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/THEME_SPLASH_SCREEN">
        <activity
            android:name=".activitys.ActivityAuth"
            android:exported="true" />
        <activity
            android:name=".activitys.ActivityInicio"
            android:exported="true" />
        <activity
            android:name=".activitys.ActivityVerification"
            android:exported="true" />
        <activity
            android:name=".activitys.ActivityAuthentication"
            android:exported="true" />
        <activity
            android:name=".activitys.ActivityWelcome"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>